
pipeline{
    agent any
    stages{
        stage("git clone"){
            steps{
                echo "https://github.com/sunitha2009/scmpipeline.git"
            }
        }
        stage("build"){
            steps{
                echo "this is build clone section"
    }    }   }  bat 'mvn clean install' 
}